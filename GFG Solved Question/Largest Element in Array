class Solution
{
public:
    int largest(vector<int> &arr, int n)
    {
        int maxi = INT_MIN;
        
        for(auto i : arr){
            if(maxi < i){
                maxi = i;
            }
        }
        
        return maxi;
    }
};
