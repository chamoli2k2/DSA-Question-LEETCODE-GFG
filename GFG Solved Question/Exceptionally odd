class Solution{   
public:
    int getOddOccurrence(int arr[], int n) {
        int XOR = arr[0];
        for(int i=1;i<n;i++){
            XOR = XOR ^ arr[i];
        }
        
        return XOR;
    }
};
