class Solution {
public:
    string finalString(string s) {
        string ans = "";
        for(auto it : s){
            if(it == 'i'){
                reverse(begin(ans),end(ans));
            }
            else{
                ans += it;
            }
        }
        return ans;
    }
};
