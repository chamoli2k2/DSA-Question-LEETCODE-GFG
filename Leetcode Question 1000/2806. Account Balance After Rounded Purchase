class Solution {
public:
    int accountBalanceAfterPurchase(int num) {
        int x = num / 10;
        int y = num % 10;
        if(y < 5) return 100 - x*10;
        return 100 - (x+1)*10;
    }
};
