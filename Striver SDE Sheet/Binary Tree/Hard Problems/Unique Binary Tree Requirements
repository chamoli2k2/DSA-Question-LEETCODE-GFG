class Solution
{
public:
    bool isPossible(int a,int b)
    {
        return a^b && (a == 2 || b == 2);
    }
};

//For Unique Binary Tree Construction we need Inorder Traversal exactly once , and other can be either preorder or postorder .
